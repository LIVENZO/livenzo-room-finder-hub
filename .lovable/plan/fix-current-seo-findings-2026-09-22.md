# Fix Current SEO Findings

## Changes
- Keep the Find Room page visually unchanged, as requested; its missing visible H1 finding will remain open because resolving it conflicts with that requirement.
- Add a public `/girls-hostels-in-kota` page using the existing room listings and cards, filtered to available female-friendly PG and hostel properties.
- Give the new page a clear H1, supporting H2, concise useful copy, unique title and description, canonical URL, social metadata, and ItemList structured data.
- Add the new public page to the generated sitemap and include natural navigation between it and the broader Find Room page.
- Preserve owner-only pages and all existing room, filter, booking, and dashboard behavior.

## Validation
- Confirm the new route is public, shows only matching listings, and displays the existing loading spinner while data loads.
- Confirm its title, description, canonical URL, social tags, headings, and structured data.
- Confirm Find Room has no new visible SEO copy and remains visually unchanged.
- Run the project type check and mark only the completed girls-hostel finding fixed.
