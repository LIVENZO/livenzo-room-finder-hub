// Authentication configuration
// Set AUTH_ENABLED to false to bypass authentication
// Set AUTH_ENABLED to true to re-enable full authentication
export const AUTH_CONFIG = {
  AUTH_ENABLED: false, // Change this to true to re-enable authentication
} as const;